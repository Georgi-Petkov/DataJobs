# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Databricks Asset Bundles / CLI
.databricks/
.databrickscfg

# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars

# secrets — never commit these
.env
*.pem
*.key

# Raw scraped job postings — never commit, this repo is public.
# /as3jobs (moved here from PKH) saves dated snapshots into data/as3_jobs_scan/.
data/

# Playwright MCP's local trace cache (console logs, page snapshots) --
# /as3jobs will create this once actually run from here. Pure local
# debugging output, regenerates automatically, never commit.
.playwright-mcp/
